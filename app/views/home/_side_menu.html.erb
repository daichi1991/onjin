
  <% sounds.each do |sound| %>
    <%=link_to sound.creator.user.username,creator_path(sound.creator) %>さんが<br>
    <%= link_to sound.title, sound_path(sound.id) %>をアップロードしました<br>
    <%= sound.created_at%><br><br>
  <% end %>
