<%= form_with(url: @price_list, local: true) do |f| %>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description%>
  </div>

  <%= f.hidden_field :creator_id, :value => @creator.id %>
  <%= f.submit nil, class: 'btn btn-primary' %>

<% end %>
